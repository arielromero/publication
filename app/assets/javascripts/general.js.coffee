
  #$( ".datepicker" ).datepicker()
  
  #$('.dropdown-toggle').dropdown()

  
  # $(document.body).on 'submit', '#new_product_subscription', (event) ->
  #   event.preventDefault()
  #   form       = $(this)
  #   loader     = container
  #   loaded     = "#product_subcriptions"
  #   #$('#new_evaluation_group_form_modal').modal('hide')
  #   $.post(form.attr('action'), form.serialize(), () ->
  #     loaded.fadeOut('slow')
  #     alert("Hola")
  #     $.get(form.data('redirect-to'), (data) ->
  #       loaded.html(data)
  #       loaded.fadeIn('slow')
      
  #     )
  #   )
  
