<h1>Listing product_subscriptions</h1>

<table>
  <tr>
    <th>Subscription</th>
    <th>Product</th>
    <th>Total</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_subscriptions.each do |product_subscription| %>
  <tr>
    <td><%= product_subscription.subscription %></td>
    <td><%= product_subscription.product %></td>
    <td><%= product_subscription.total %></td>
    <td><%= link_to 'Show', product_subscription %></td>
    <td><%= link_to 'Edit', edit_product_subscription_path(product_subscription) %></td>
    <td><%= link_to 'Destroy', product_subscription, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product subscription', new_product_subscription_path %>
