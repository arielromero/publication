<%= simple_form_for(@product_subscription) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :subscription %>
    <%= f.association :product %>
    <%= f.input :total %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
