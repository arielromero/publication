<div class="app-header row-fluid">
  <div class="app-logo span6">
  </div>
  <div class="head-btns span6">
    <%= link_to t('campaign.title'), campaigns_path, :class => "btn btn-warning pull-right" %>
    <%= link_to t('product.title'), products_path, :class => "btn btn-warning pull-right" %>
    <%= link_to t('member.title'), root_url, :class => "btn btn-warning pull-right" %>
    <% if user_signed_in? %>
    <div class="btn-group">
      <button class="btn dropdown-toggle" data-toggle="dropdown">
        <%= current_user.email  %>
        <span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
        <li>
        <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>        
        </li>
    <!-- dropdown menu links -->
      </ul>
    </div>  
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>  
  </li>
<% end %>
  </div>
</div>